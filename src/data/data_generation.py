import pandas as pd


def get_test_data(start_date: str, end_date: str) -> pd.DataFrame:
    """Generate test dataset performing data extraction and preprocessing

    Args:
        start_date (str): Date in %Y%m%d format
        end_date (str): Date in %Y%m%d format

    Returns:
        pd.DataFrame: Test dataset
    """

    return pd.DataFrame()


def get_train_data(start_date: str, end_date: str) -> pd.DataFrame:
    """Generate train dataset performing data extraction and preprocessing

    Args:
        start_date (str): Date in %Y%m%d format
        end_date (str): Date in %Y%m%d format

    Returns:
        pd.DataFrame: Train dataset
    """

    return pd.DataFrame()
